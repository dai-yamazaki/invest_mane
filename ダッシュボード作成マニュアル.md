# 投資ダッシュボード 作成マニュアル

作成日: 2026年6月1日

---

## 概要

Google Drive に保存している楽天証券データ（スプレッドシート「日本株管理」）と  
Yahoo Finance Japan のスクレイピングデータを組み合わせて、  
MoneyForward 風の投資ポートフォリオダッシュボードを HTML として作成した。

**成果物:** `投資ダッシュボード.html`（ブラウザで開くだけで動作、サーバー不要）

---

## 使用ツール・技術

| ツール | 用途 |
|--------|------|
| Claude Code（VSCode拡張） | 実装・ファイル作成 |
| Google Drive MCP | スプレッドシートのデータ読み取り |
| Yahoo Finance Japan | 配当データのスクレイピング元 |
| Python 3（requests + BeautifulSoup） | 配当スクレイピングスクリプト |
| Chart.js (v4.4.0) | グラフ描画（CDN読み込み） |
| HTML / CSS / JavaScript | ダッシュボード本体 |

---

## Step 1：Google Drive MCP の接続確認

Claude の claude.ai チャット側で Google Drive を連携済みの状態で、  
Claude Code（VSCode拡張）から MCP ツールが利用できることを確認した。

### 利用した MCP ツール

```
mcp__claude_ai_Google_Drive__list_recent_files   # ファイル一覧取得
mcp__claude_ai_Google_Drive__read_file_content   # ファイル内容読み取り
```

### 接続確認の手順

1. `list_recent_files` でDrive内のファイル一覧を取得
2. 「日本株管理」スプレッドシートが見えることを確認
3. `read_file_content` にファイルIDを渡してデータ取得

```
ファイルID: 1b3Hfdt4R51y7A55LECap7K-uTN_hjd8JMnKMF33_vAU
URL: https://docs.google.com/spreadsheets/d/1b3Hfdt4R51y7A55LECap7K-uTN_hjd8JMnKMF33_vAU/
```

> **ポイント:** MCP ツールは「deferred tools」として登録されており、  
> `ToolSearch` で `select:ツール名` を実行してスキーマを取得してから呼び出す必要がある。

---

## Step 2：スプレッドシートのデータ構造把握

`read_file_content` で取得したデータには以下のシート/テーブルが含まれていた。

### ① master テーブル（銘柄マスタ）
- 銘柄名・判定（国内高配当/国内グロース/米国インデックスなど）・セクター
- **⚠️ 配当データは古い可能性が高いため使用しない → Step 3 でスクレイピングに差し替え**

### ② 個別株式テーブル（masterシート内）
```
コード | 銘柄名 | セクター | 保有数量 | 現在値 | 時価評価額 | 予想一株配当 | 年間配当金 | 配当利回り
```
- 22銘柄分（NISA成長投資枠）
- **⚠️ 配当データは手動入力のため古くなりがち → Step 3 に置き換え**

### ③ ALL Raw テーブル（楽天CSVエクスポートデータ）★主要
```
種別 | コード | 銘柄名 | 口座 | 保有数量 | 平均取得価額 | 現在値 | 時価評価額 | 評価損益 | 損益率 | 株式種別
```
- 国内株式・米国株式・投資信託 全ポジションの詳細データ
- **株価・保有数量・評価損益はこのテーブルを使用**

### ④ 株式種別サマリーテーブル
```
株式種別 | 時価評価額合計 | 構成比 | 評価損益合計
```
- 米国インデックス・世界インデックス・国内高配当など8カテゴリ

---

## Step 3：配当データのスクレイピング（★重要）

masterシートの配当データは古い可能性が高いため、  
**Yahoo Finance Japan から直接スクレイピング**して取得する。

### スクレイピングスクリプト

`fetch_dividends.py` を実行する。

```bash
cd "/Users/daiyamazaki/Desktop/Investment management"
python3 fetch_dividends.py
```

### 取得するデータ

| 項目 | 取得元URL | 内容 |
|------|-----------|------|
| 今期予想配当（1株） | `https://finance.yahoo.co.jp/quote/{code}.T` | 会社予想の1株配当 |
| 配当利回り | 同上 | 会社予想ベース |
| 前年度実績配当（1株） | `https://finance.yahoo.co.jp/quote/{code}.T/dividend` | 配当履歴の直近実績 |

### パース方法

**今期予想・利回り（株価ページ）:**
```python
for dl in soup.find_all('dl', class_=lambda c: c and 'DataListItem_' in str(c)):
    dt, dd = dl.find('dt'), dl.find('dd')
    label, value = dt.get_text(strip=True), dd.get_text(strip=True)
    if '1株配当' in label:     → value から数値を正規表現で抽出
    if '配当利回り' in label:  → value から XX.XX% を抽出
```

**前年度実績（配当履歴ページ）:**
```python
tables = soup.find_all('table')
# table[1] = 実績テーブル（最新年度が先頭行）
rows = tables[1].find_all('tr')
# 先頭の "YYYY年M月期" 行の年間配当列を取得
```

### スクレイピング結果の例（2026年6月1日）

| 銘柄 | 前年実績 | 今期予想 | 増配率 |
|------|---------|---------|------|
| アステラス製薬 | 78円 | 80円 | +2.6% |
| 三井住友FG | 157円 | 180円 | +14.6% |
| 任天堂 | 219円 | 162円 | **-26.0%** |
| ジャパンInvest.A | 87円 | 108円 | +24.1% |
| 三菱UFJ | 86円 | 96円 | +11.6% |

---

## Step 4：データの分類・集計

スプレッドシートのデータを以下の4カテゴリに再分類した。

| カテゴリ | 対象銘柄 | 時価評価額 |
|----------|----------|-----------|
| 投資信託・ETF | 投資信託全種 + 国内ETF（1489/1545/1557） | ¥7,409,191 |
| 個別株式 | 国内個別株22銘柄（ETF除く） | ¥1,238,670 |
| ゴールド | 純金上場信託(1540) / IAU / GLDM | ¥749,548 |
| 債券 | バンガード超長期米国債(EDV) | ¥406,406 |
| **合計** | | **¥9,803,815** |

---

## Step 5：ダッシュボードの実装

### ファイル構成

```
Desktop/Investment management/
├── 投資ダッシュボード.html          ← メイン成果物（単一ファイル完結）
├── fetch_dividends.py               ← 配当スクレイピングスクリプト
├── ダッシュボード作成マニュアル.md  ← このファイル
└── ダッシュボード作成マニュアル.html ← ブラウザ表示用マニュアル
```

### HTML の構成セクション

```
1. ヘッダー
   - 時価評価額合計・評価損益・年間配当金予想（スクレイピング値）

2. カテゴリサマリーカード（4枚）
   - 投資信託・ETF / 個別株式 / ゴールド / 債券

3. ドーナツチャート × 2
   - アセット配分（カテゴリ別）
   - アセット配分（投資種別：米国インデックス/世界インデックス等）

4. 投資信託・ETF 一覧テーブル
   - 全ファンド・ETFの損益率付き一覧

5. 個別株式 一覧テーブル
   - 配当利回りを色分け（4.5%以上=緑、3%以上=青）

6. グラフ × 2（横並び）
   - セクター別ドーナツ（個別株）
   - 銘柄別年間配当バーチャート（横棒）

7. 月別配当金カレンダー
   - 各銘柄の決算月から支払月を推定してバー表示

8. 配当データ詳細セクション（★新規）
   - 取得元: Yahoo Finance Japan へのリンク付き
   - KPIストリップ: 年間配当合計・前年実績・差額・増配/減配銘柄数
   - 比較テーブル: 前年度実績 vs 今期予想 / 増配額 / 増配率（前年比）/ 取得元URL
```

### 主要な JavaScript データ定義

```javascript
// 個別株式データ（22銘柄）
// div      = 今期予想年間配当合計[円]（スクレイピング値 × 保有数量）
// prevDiv  = 前年度実績年間配当合計[円]（Yahoo Finance履歴 × 保有数量）
// divUrl   = 取得元URL（Yahoo Finance Japan）
const stocks = [
  { code:'4503', name:'アステラス製薬', sector:'医薬品',
    qty:100, price:2174, value:217400, pnl:52450, pnlPct:31.79,
    div:8000, yield:3.68, prevDiv:7800,
    divUrl:'https://finance.yahoo.co.jp/quote/4503.T' },
  // ...
];

// 投資信託・ETFデータ（評価損益はスプレッドシートのALL Rawから）
const funds   = [ ... ];
const goldETFs = [ ... ];
const bondETFs = [ ... ];
```

### 使用ライブラリ

```html
<!-- グラフ描画（CDN・インストール不要・インターネット接続が必要） -->
<script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
```

```bash
# スクレイピング（pip でインストール済み）
pip3 install requests beautifulsoup4
```

---

## Step 6：データ更新方法

### 方法A：`/update-dashboard` コマンドを使う（推奨）

```
/update-dashboard
```

自動実行される処理:
1. `fetch_dividends.py` を実行 → Yahoo Finance Japan から配当データ（今期予想・前年実績）取得
2. Google Drive MCP でスプレッドシートを読み込み → 株価・評価損益・保有数量取得
3. HTML の全データを更新

### 方法B：スクリプトを手動実行してから依頼

```bash
cd "/Users/daiyamazaki/Desktop/Investment management"
python3 fetch_dividends.py 2>/tmp/div.txt; cat /tmp/div.txt
```

出力を確認後、「ダッシュボードを更新して」と依頼する。

### 方法C：HTML を直接編集する

`投資ダッシュボード.html` を VSCode 等で開き、以下の2箇所を書き換える。

```javascript
const stocks = [ ... ]  // div, prevDiv, yield を更新
const funds  = [ ... ]  // value, pnl, pnlPct を更新
```

---

## トラブルシューティング

| 問題 | 対処法 |
|------|--------|
| グラフが表示されない | インターネット接続を確認（Chart.js はCDN読み込み） |
| 配当スクレイピングが N/A | Yahoo Finance Japan の HTML 構造変更の可能性。`fetch_dividends.py` のパース部分を確認 |
| MCP ツールが使えない | claude.ai で Google Drive の連携状態を確認する |
| スクレイピングが全銘柄 N/A | `requests` / `beautifulsoup4` のインストール確認: `pip3 install requests beautifulsoup4` |

---

## 参考：ファイル・URL一覧

| 種別 | 内容 |
|------|------|
| スプレッドシート | `日本株管理`（Google Drive / 資産管理フォルダ） |
| スプレッドシートID | `1b3Hfdt4R51y7A55LECap7K-uTN_hjd8JMnKMF33_vAU` |
| 配当取得元（株価） | `https://finance.yahoo.co.jp/quote/{code}.T` |
| 配当取得元（履歴） | `https://finance.yahoo.co.jp/quote/{code}.T/dividend` |
| データソース | 楽天証券（CSVエクスポート → スプレッドシートに貼り付け） |
